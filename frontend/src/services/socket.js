/**
 * Real-time transport. Install `socket.io-client` and initialize `io()` here when ready.
 * @returns {import('socket.io-client').Socket | null}
 */
export function getSocket() {
  return null
}

export function disconnectSocket() {}
